/*public class Car {
    private String make;
    private String model;
    private int year;
    boolean isRunning;
   /*public void startCar(String make,String model,int year,Boolean isRunning)
   {
    this.make=make;
    this.model=model;
    this.year=year;
    this.isRunning=isRunning;
    System.out.println("Brand of car is "+make+" Model of car is "+model+" Year of car is "+year+" Car is running "+isRunning);
   }
   public  void stopCar(String make,String model,int year,Boolean isRunning)
   {
       this.make=make;
       this.model=model;
       this.year=year;
       this.isRunning=isRunning;
       System.out.println("Brand of car is "+make+" Model of car is "+model+" Year of car is "+year+" Car is not running "+isRunning);
   }*/

  /*  public Car(String make, String model, int year, Boolean isRunning) {
        this.make = make;
        this.model = model;
        this.year = year;
        this.isRunning = isRunning;
    }
    public boolean startCar()
    {
        if (!isRunning)
            return false;
    }

    public static void main(String args[])
    {
       Car c = new Car("Nissan","Sentra",2020,true);
       //c.startCar("Nissan","Sentra",2020,true);
        //c.startCar("Nissan","Sentra",2020,false);
        boolean stat=c.startCar();
        System.out.println(stat);

    }
}*/
